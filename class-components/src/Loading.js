import React from 'react';

const Loading = () => {
    return (
        <div className="ui active dimmer">
            <div className="ui text loader">Getting Location...</div>
        </div>
    );
}

export default Loading;